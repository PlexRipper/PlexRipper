export enum MediaSortField {
	Title = 'sortIndex', // This is sort index by title
	Year = 'year',
	AddedAt = 'addedAt',
	UpdatedAt = 'updatedAt',
	Duration = 'duration',
	MediaSize = 'mediaSize',
	Quality = 'quality',
}

export enum SortDirection {
	Asc = 'asc',
	Desc = 'desc',
	NoSort = 'no-sort',
}
