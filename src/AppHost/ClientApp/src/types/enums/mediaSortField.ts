export enum MediaSortField {
	Title = 'title',
	Year = 'year',
	AddedAt = 'addedAt',
	UpdatedAt = 'updatedAt',
	Duration = 'duration',
	MediaSize = 'mediaSize',
	Index = 'sortIndex',
	Quality = 'quality',
}

export enum SortDirection {
	Asc = 'asc',
	Desc = 'desc',
	NoSort = 'no-sort',
}
